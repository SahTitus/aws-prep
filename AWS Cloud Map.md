# **AWS Cloud Map**
 is a **service discovery tool** that lets applications **register, map, and dynamically discover resources** like microservices or databases using **custom names**, automatically updating their **locations** as they change.

👉 In short: 
* **AWS Cloud Map** = **“Dynamic service discovery for your AWS resources.”**